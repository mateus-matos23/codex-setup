interface:
  display_name: "Project Context"
  short_description: "Map setup and validation commands"
  default_prompt: "Use $project-context to map this unfamiliar repository before implementation."
policy:
  allow_implicit_invocation: false
